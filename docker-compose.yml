services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: webhookbridge
      POSTGRES_PASSWORD: webhookbridge
      POSTGRES_DB: webhookbridge
    ports:
      - "5434:5432"
    volumes:
      - webhookbridge_db:/var/lib/postgresql/data

volumes:
  webhookbridge_db:

